<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_detail"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorPrimaryBackground"
    tools:context="com.mavpokit.transitionsapp.activities.DetailActivity">
    <!--android:background="@drawable/fish1"-->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/fish"
        android:transitionName="sharedFish"
        android:layout_margin="40dp"
        android:id="@+id/imageviewFishSharedElement"/>
        <!--to make shared elenent animation, id's of shared elements must be the same -->
    <TextView
        android:text="Detail Activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="50sp"
        android:textStyle="bold|italic"
        android:textColor="@color/colorPrimary"
        android:id="@+id/textView3" />
</RelativeLayout>
